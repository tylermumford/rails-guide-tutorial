<h1>Articles</h1>

<p>There are <%= Article.public_count %> published articles so far.</p>

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
                <% unless article.status == 'public' %>[<%= article.status %>]<% end %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New article...", new_article_path %>
